@model ReservationSystem.ViewModels.SittingViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Sitting</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Sitting.BookingId" class="control-label"></label>
                <br/>
                @Html.DropDownListFor(model => model.Sitting.BookingId, new SelectList(Model.Reservation, "BookingId", "BookingId"),
                new {@class ="form-control from-select"})
            </div>
            <div class="form-group">
                <label asp-for="Sitting.TableId" class="control-label"></label>
                <br/>
                @Html.DropDownListFor(model => model.Sitting.TableId, new SelectList(Model.Table,"Id", "TableNum"),
                new {@class="form-control from-select"})
            </div>
            @*<div class="form-group">
                <label asp-for="ResDate" class="control-label"></label>
                <select asp-for="ResDate" class ="form-control" asp-items="ViewBag.ResDate"></select>
            </div>
            <div class="form-group">
                <label asp-for="StartTime" class="control-label"></label>
                <select asp-for="StartTime" class ="form-control" asp-items="ViewBag.StartTime"></select>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
